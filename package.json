{
  "name": "kw-chatbot-frontend-app",
  "version": "1.0.0",
  "description": "Capacitor wrapper for the KW AI Chatbot.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx cap sync"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
